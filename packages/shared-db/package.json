{
  "name": "@url-shortener/shared-db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate": "bun run ./migrate.ts",
    "backup": "bun run ./backup.ts",
    "studio": "bunx drizzle-kit studio",
    "typecheck": "tsgo -p tsconfig.json --noEmit",
    "lint": "biome lint client.ts schema.ts drizzle.config.ts migrate.ts backup.ts",
    "format": "biome format client.ts schema.ts drizzle.config.ts migrate.ts backup.ts --write"
  },
  "exports": {
    "./schema": "./schema.ts",
    "./client": "./client.ts"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.0"
  },
  "dependencies": {
    "drizzle-orm": "^0.43.0"
  }
}
