FROM oven/bun:1.3-debian

WORKDIR /app

COPY package.json bun.lock ./
COPY packages ./packages
COPY apps ./apps

RUN bun install --frozen-lockfile

ENV NODE_ENV=production
ENV HOST=0.0.0.0
EXPOSE 3000

CMD ["bun", "run", "apps/redirect/src/server.ts"]
